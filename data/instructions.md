1. Download dataset from https://www.kaggle.com/datasets/sahandnamvar/phishing-url-detection-111k-urls-22-features?resource=download
2. Insert in directory `/data`.
3. Name original dataset as `Dataset.csv`.
3. Run script `/scripts/normalization.py` to generate normalized dataset.

Note: You need to run scripts in the script directory so relative paths work correctly.